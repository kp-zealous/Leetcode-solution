class Solution(object):
    def isPalindrome(self, s):
        """
        :type s: str
        :rtype: bool
        """
        a = ''.join(c for c in s if c.isalnum())
        a=a.lower()
        if a==a[::-1]:
            return True
        else:
            return False
